<%= t('.welcome', email:@resource.email ) %>

<%= t('.request_send') %>

<%= t('.didnt_request') %>

<%= link_to t('.reset_password'), edit_password_url(@resource, reset_password_token: @token, config: message['client-config'].to_s, redirect_url: message['redirect-url'].to_s, allow_other_host: true) %>
